## 风险清单（规范 v2.0 与工具脚本 schema 分叉）

| 文件路径/范围 | 风险点 | 影响 | 处置建议 |
| --- | --- | --- | --- |
| `tools/test_dxf_roi_extract.py` | 仍按旧版 `spec["sections"]["titleblock_extraction_spec"]` 读取 schema | 无法直接加载当前 `参数规范.yaml` (v2.0 顶层 `titleblock_extract`)，导致回归/调试脚本失效或读错ROI配置 | 建议新增统一 Spec Loader，适配 v2.0 顶层字段；脚本改为通过 loader 获取 `titleblock_extract` 段 |
| `tools/check_md_yaml_correspondence.py` | 仍使用 `spec["sections"]["doc_generation_spec"]`、旧对象名 | 比对 MD 与 YAML 时会报缺键/误报，难以验证文档一致性 | 同上，引入适配层或更新键路径到 `doc_generation` 顶层 |
| `tools/round_yaml_floats.py` | 仅在 `sections.titleblock_extraction_spec...` 范围内做数值统一 | 对 v2.0 顶层 `titleblock_extract` 不再生效，可能漏掉标定数值校正 | 调整路径匹配到现行顶层结构，或让脚本通过统一 loader 获取需要 round 的节点 |
| `documents/参数规范.yaml` (`discipline_code_map` / `status_to_design_phase`) | 专业代码/状态映射仍仅列少量值（“其他专业待补充”已去掉注释但未补全数据） | 设计文件/IED 的专业代码、设计阶段可能为空或错误，影响下游校验 | 补全映射或提供默认/兜底策略，并在校验时显式告警 |
| `documents_bin/responsible_unit.json` | 仅 3 条责任单位示例，明显不全 | IED 前端下拉/必填字段可能无法覆盖实际值，发布状态下易校验失败 | 补全责任单位列表或在前端标注为示例/可自由输入；后端允许自定义值并记录来源 |
| `documents_bin/responsible_unit.json` | 仅 3 条责任单位示例，明显不全 | IED 前端下拉/必填字段可能无法覆盖实际值，发布状态下易校验失败 | 补全责任单位列表或在前端标注为示例/可自由输入；后端允许自定义值并记录来源 |
